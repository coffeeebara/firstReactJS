import { Metadata } from "next";

export const metadata: Metadata = {
    title: "not found",
};

export default function NotFound() {
    return (
        <div>
            <h1>not foundd</h1>
        </div>
    );
}